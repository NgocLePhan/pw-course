// khai báo biến
let number = -6;

//kiểm tra hằng số lớn hơn 0
if (number > 0) {
    console.log("Giá trị bạn nhập là dương");
}

// kiểm tra hằng số nhỏ hơn 0
if (number < 0) {
    console.log("Giá trị bạn nhập là âm");
}

// kiểm tra hằng số bằng 0
if (number == 0) {
    console.log("Giá trị bạn nhập là số 0");
}