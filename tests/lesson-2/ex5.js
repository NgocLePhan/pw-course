// Lặp từ 1 tới 100
for (let i = 1; i <= 100; i++) {
    // Kiểm tra số chẵn
    if (i % 2 === 0) {
        console.log("Số " + i + " là số chẵn.");
    }

    // kiểm tra số lẽ
    if (i % 2 === 1) {
        console.log("Số " + i + " là số lẽ.");
    }

}